name: 01 - Building Blocks 

on: push

jobs: 
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: say hello
              run: echo "hello words"
    echo-goodbye:
        runs-on: ubuntu-latest 